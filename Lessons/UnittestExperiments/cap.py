def cap_text(text) :
    """
    Input a string then output the capitalized string.
    """
    # Using title() capitalizes each word within a given string. As opposed to capitalize() which only capitalizes the first word in a given string.
    return text.title()