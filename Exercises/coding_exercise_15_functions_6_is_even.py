# Define a function called is_even that takes in one argument and retirns True if the passed in value is even and False if it is not.
def is_even(x) :
    if x %2 == 0 :
        return True
    else :
        pass
    return False